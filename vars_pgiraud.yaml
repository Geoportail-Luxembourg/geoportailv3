extends: vars.yaml

vars:
    # overwrite dbport for the ssh tunnel to the db
    dbport: 9999

    dbsessions:
      mymaps:
        url: 'postgresql://www-data:www-data@localhost:5432/mymaps'
      ecadastre:
        url: 'postgresql://user:password@db.geoportal.lu/ecadastre'
