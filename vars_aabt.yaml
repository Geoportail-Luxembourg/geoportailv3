extends: vars_geoportailv3.yaml

vars:
    # overwrite dbport for the ssh tunnel to the db
    dbport: 9999

    sqlalchemy_engines:
      mymaps: 'postgresql://www-data:www-data@localhost:5432/mymaps'
      ecadastre: 'postgresql://user:password@db.geoportal.lu/ecadastre'
